services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: kasa 
      POSTGRES_PASSWORD: kasa
      POSTGRES_DB: kasa
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: 
